# 开发日志

## 2025-06-12 ~ 2025-06-14

### 项目基础
- SwiftUI + Swift | macOS 14+ | Xcode 26.5 | DeepSeek API | Vision 框架
- 22 源文件，零警告零错误

### 核心功能
- 截图/上传 → 框选选区 → OCR（Vision 本地，不依赖 API）→ 翻译（DeepSeek）→ 分析
- 翻译分析手动触发，OCR 自动运行
- 模式切换自动触发翻译（已有 OCR 时）

### 图片框选
- 拖拽创建选区、8 手柄精调（4 角 + 4 边）、选区内部拖拽移动
- SF Symbol 对角光标 + 十字星 + 手掌拖动
- 缩放 100%~500%：滚轮 + 触控板捏合 + 按钮，累计连续
- 平移不限缩放，80px 边界保留，触控板滑动 vs 鼠标滚轮自动识别
- 百分比坐标映射裁剪，避免累积误差
- 滚动事件仅响应图片区域（FrameReader 命中检测），Sheet 弹出时跳过

### 分析模式
- **校对模式**：精准句子成分拆解，无语法/词汇，Markdown 表格展示
- **学习模式**：句子成分 + 语法点（附例句）+ 词汇注解（仅 N1~N2）
- 模式缓存，切换模式即时显示
- 工具栏内嵌 linear 进度条

### 词汇查询
- 查词输入框 + 蓝色查询按钮，支持 Cmd+K 快速聚焦
- 浮动卡片展示结果（读音/释义/词性/JLPT/例句），折叠/展开
- 查询缓存
- 查词栏固定在翻译和分析之间，滚动时始终可见

### 翻译结果
- 原文 + 中文双区展示，原文去除换行空格
- 原文可选中复制
- 句子分析原文可选中

### UI 细节
- 三栏布局：图片 | OCR 识别 | 翻译+分析
- 初始界面「上传图片」蓝色高亮
- 工具栏固定高度，文字单行不换行
- 缩放按钮：1:1 | − | % | +
- API Key 用 UserDefaults 存储
- 光标样式：角/边/内部/外部区分

### 当前文件结构
```
OCR-Translate/
├── App/          OCR_TranslateApp.swift, Info.plist
├── Models/       OCRResult, TranslationResult, AnalysisResult
├── Services/     OCRService, DeepSeekService, TranslationService,
│                 AnalysisService, VocabularyService
├── Utilities/    ScreenshotManager, APIKeyStore, ImageLoader
└── Views/        ContentView, CroppableImageView, OCRResultView,
                  TranslationResultView, AnalysisView, SettingsView,
                  VocabularyLookupView
```

## 2025-06-15

### 完成
- [x] 多模型 API 支持：DeepSeek / ChatGPT (OpenAI) / Gemini (Google)，设置中单选切换
- [x] AIService 根据提供商自动分发到 OpenAI 兼容接口或 Gemini 原生接口
- [x] 设置窗口关于信息更新：OCR 翻译校对工具 | NJRin | 虹之咲学园 Vibe Coding 同好会
- [x] Picker 右键菜单禁用（图标/文本选项移除）
- [x] 查词卡片全部文本可选中复制
- [x] 设置面板滚动不再穿透到图片区域（sheet 检测改用 mainWindow 比对）
- [x] 识别结果增加「连贯/分段」切换按钮
- [x] 模式切换自动触发翻译（已有 OCR 时）

### 已知问题
- [ ] **查词卡片内 notes 展开后与例句重叠**：长文本用法说明展开时，未正常推动下方例句下移，文字交叠。尝试了 `lineLimit` 和 `zIndex` 均未解决
- [ ] **选框裁剪准确度**：框选选区后，OCR 识别结果似乎漏掉一部分文字。需排查 `cropImage` 中视图坐标 → 像素坐标的换算（缩放/平移/原点偏移等因子是否全部正确补偿）

### 待办
- [ ] 优化句子成分划分样式（当前 Markdown 表格效果需要改进）
- [ ] Archive 导出 Release .app

